pipeline {
    agent any

    stages {
        stage('Integration') {
            steps {
                echo 'Integration tests'
                sh 'source venv/bin/activate'
            }
        }
    }
}