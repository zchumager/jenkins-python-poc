pipeline {
    agent any

    stages {
        stage('Integration') {
            steps {
                echo 'Integration tests'
                sh './venv/bin/activate'
            }
        }
    }
}